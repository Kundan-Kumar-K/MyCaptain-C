include<stdio.h>
main()
{
	int a,b,temp;
	printf("Enter the value for a & b respectively \n ");
	scanf("%d %d",&a,&b);
	printf("The values of a & b are \n a = %d \n b = %d \n",a,b);
	temp = a;
	a = b;
	b = temp;
	printf("After swapping, the values of a & b are\n a = %d \n b = %d \n",a,b);
	return 0;
}
